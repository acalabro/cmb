package it.cnr.isti.connect.cmb.eventprocessing;

import it.cnr.isti.connect.cmb.eventprocessing.SimpleEvent;

//global java.util.List<SimpleEvent> results;

declare SimpleEvent
	@role( event )
end

rule "test entry point"
dialect "mvel"
when
	e : SimpleEvent( data = "default payload" ) from entry-point "EventStream"
then
	//results.add( e )
	System.out.println("ciao");
end