import com.sample.Message;
 
rule "Filtro"
	when
		m : Message(Message.getMessage == "Messaggio da filtrare");
	then
		m.setMessage( "Messaggio filtrato" );
		update( m );
		System.out.println(m.getMessage());
		
end