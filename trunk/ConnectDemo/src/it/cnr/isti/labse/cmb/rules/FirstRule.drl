import it.cnr.isti.labse.cmb.event.SimpleEvent;

declare SimpleEvent
	@role( event )
	@timestamp( timestamp )
end

rule "Filter"
	when
		$objA : SimpleEvent();
		$objB : SimpleEvent($objA.data == $objB.data);
	then
		System.out.println($objA.getData() + " E' UGUALE A " + $objB.getData());
end

